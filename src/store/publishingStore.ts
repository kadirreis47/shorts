import { create } from 'zustand';
import { persist } from 'zustand/middleware';
import { createPersistentStorage } from '@/persistence/storeStorage';
import { normalizePublishQueue, type PublishJob, type PublishQueueSnapshot } from '@/core/publishing';
interface PublishingState { queue: PublishQueueSnapshot; selectedJobId: string | null; lastError: string | null; updateJob: (job: PublishJob) => void; setQueue: (queue: PublishQueueSnapshot) => void; selectJob: (jobId: string | null) => void; setError: (message: string | null) => void; }
export const usePublishingStore = create<PublishingState>()(persist((set) => ({ queue: { jobs: [], activeJobId: null, paused: false }, selectedJobId: null, lastError: null, updateJob: (job) => set((state) => { const index = state.queue.jobs.findIndex((item) => item.id === job.id); const jobs = index < 0 ? [...state.queue.jobs, job] : state.queue.jobs.map((item, i) => i === index ? job : item); const active = ['uploading', 'processing', 'verifying', 'reconciling'].includes(job.state) ? job.id : state.queue.activeJobId === job.id ? null : state.queue.activeJobId; return { queue: { ...state.queue, jobs, activeJobId: active } }; }), setQueue: (queue) => set({ queue: { ...queue, activeJobId: null } }), selectJob: (selectedJobId) => set({ selectedJobId }), setError: (lastError) => set({ lastError }) }), { name: 'shortsflow-publishing', version: 1, storage: createPersistentStorage(), skipHydration: true, partialize: (state) => ({ queue: { ...state.queue, activeJobId: null } }), onRehydrateStorage: () => (state) => { if (state) state.setQueue(normalizePublishQueue(state.queue)); }, migrate: (persisted) => { const value = persisted as Partial<PublishingState>; return { queue: normalizePublishQueue(value.queue ?? { jobs: [], activeJobId: null, paused: false }), selectedJobId: null, lastError: null }; } }));
