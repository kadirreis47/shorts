import type { AudioProductionPlan, AudioProductionPreview } from '@/core/audio-production'; import { Card } from '@/components/ui';
export function AudioPlanSummary({ plan, preview }: { plan: AudioProductionPlan; preview: AudioProductionPreview }) { const scores = [['Voice', plan.quality.overallVoiceScore], ['Pacing', plan.pacing.paceScore], ['Alignment', average(plan.voiceAnalysis.map((item) => item.alignmentScore))], ['Quality gain', plan.expectedQualityImpact]] as const; return <div className="grid gap-3 sm:grid-cols-2 xl:grid-cols-4">{scores.map(([label, score]) => <Card className="p-4" key={label}><p className="text-xs uppercase text-slate-500">{label}</p><p className="mt-1 text-2xl font-semibold">{score}</p></Card>)}<Card className="p-4 sm:col-span-2"><p className="text-sm font-medium">Preview</p><p className="text-sm text-slate-500">{preview.affectedScenes.length} affected scenes · {preview.removedSilenceMs} ms silence removed · {preview.automation.length} automation points</p></Card><Card className="p-4 sm:col-span-2"><p className="text-sm font-medium">Loudness profile</p><p className="text-sm text-slate-500">{plan.loudness.profile}: {plan.loudness.finalTargetLufs} LUFS · peak {plan.loudness.truePeakDb} dB</p></Card></div>; }
function average(values: readonly number[]): number { return values.length ? Math.round(values.reduce((a, b) => a + b, 0) / values.length) : 0; }
