import type { EditOperation } from '@/core/editing';
import { Card } from '@/components/ui';
export function EditOperationList({ operations, onToggle }: { operations: readonly EditOperation[]; onToggle(id: string, enabled: boolean): void }) { return <Card className="p-4"><h2 className="font-semibold">Edit operations</h2><div className="mt-3 space-y-2">{operations.map((operation) => <label key={operation.id} className="flex gap-3 rounded-lg border p-3"><input type="checkbox" checked={operation.status !== 'disabled'} onChange={(event) => onToggle(operation.id, event.target.checked)} /><span className="min-w-0"><span className="font-medium">{operation.type}</span><span className="ml-2 rounded bg-slate-100 px-2 py-0.5 text-xs">{operation.automaticallyApplicable ? 'automatic' : 'manual'}</span><p className="mt-1 text-sm text-slate-500">{operation.reason}</p><p className="mt-1 text-xs text-slate-400">Scene: {operation.sceneId ?? 'global'} · confidence {operation.confidence}</p></span></label>)}</div></Card>; }
